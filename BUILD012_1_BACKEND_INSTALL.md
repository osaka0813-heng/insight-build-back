# Build012.1 backend installation

Copy both files into the backend repository:

- `api/publish.mjs`
- `lib/worldProcessFoundation.mjs`

Then deploy on Vercel.

The helper runs only after an approved Writer Draft has already been merged by the existing publisher. It does not change Research, Analyze, or Write endpoints.
