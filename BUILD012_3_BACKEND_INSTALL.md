# Build012.3 backend installation

1. Copy `api/admin-verify.mjs` into the backend repository.
2. Add `ADMIN_CONSOLE_TOKEN` in Vercel Environment Variables.
3. Use a long private value different from Research and Publish tokens.
4. Redeploy the backend.

Existing Build012.2 backend files remain unchanged.
